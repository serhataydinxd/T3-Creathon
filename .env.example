# The public demo works without environment variables in replay mode.
APP_MODE=replay

# Required when persistence is enabled.
DATABASE_URL=postgresql://imkan:change-me@localhost:5432/imkan

# Never commit provider credentials.
LLM_API_KEY=
